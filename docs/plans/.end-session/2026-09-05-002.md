---
type: session
session-id: 2026-09-05-002
timestamp: "2026-09-05T17:27:32Z"
commit-sha: "86e34d2"
agent: "pi"
files-changed: 34
status: complete
---

# Session End: Run plan and work skills in the agent pipeline

## Reason

The user re-pointed `agent/` at the repository's own workflow skills: the planning stage should
**execute** `.agents/skills/plan` and the implementation stage should **run** `/work`, everything in
autopilot, task files always generated, and without the intermediate phase dumps.

While orienting, two gaps surfaced that made the change bigger than a prompt tweak: `when-to-use`
was a required field in `discoverSkills`, so every arreio skill was being skipped with a warning —
and `buildGeneratorPrompt` (T07's prompt library) was dead code, because the loop built its own
generic `"You are a code generation assistant"` system turn, so the derived rules, exemplars and any
procedure never reached the model. Three decisions were confirmed with the user before writing code:
target the `agent/` CLI (not session conventions), drop `.scope/.research/.design/.work` dumps, and
implement fully. The `advisor` reviewed the approach before implementation and its guardrails held:
append skill text rather than replace the output contract, widen the plan schema instead of
redesigning the forced `write_plan` call, and verify in two passes.

Plan 2026-09-05-001 was written down afterwards at the user's request, through the plan skill's
Generate + Tasks phases in autopilot.

## Changed Files

### Commit `961f6ed` — Run plan and work skills in the agent pipeline (16 files)

- `agent/src/config.ts` (modified)
- `agent/src/generator.ts` (modified)
- `agent/src/index.ts` (modified)
- `agent/src/plan-artifacts.ts` (added)
- `agent/src/plan-schema.ts` (modified)
- `agent/src/plan.ts` (modified)
- `agent/src/prompts/generator.ts` (modified)
- `agent/src/prompts/planner.ts` (modified)
- `agent/src/skill-prompts.ts` (added)
- `agent/src/skills.ts` (modified)
- `agent/src/work-artifacts.ts` (added)
- `agent/tests/generalization.test.ts` (modified)
- `agent/tests/plan-artifacts.test.ts` (added)
- `agent/tests/prompts.test.ts` (modified)
- `agent/tests/skills.test.ts` (modified)
- `agent/tests/work-artifacts.test.ts` (added)

### Commit `86e34d2` — Record plan 2026-09-05-001 and its 15 tasks (18 files)

- `docs/plans/2026-09-05-001-agent-runs-plan-and-work-skills.md` (added)
- `docs/plans/index.md` (modified)
- `docs/tasks/2026-09-05-001/T01-green-baseline-agent-typecheck.md` (added)
- `docs/tasks/2026-09-05-001/T02-discover-skills-without-when-to-use.md` (added)
- `docs/tasks/2026-09-05-001/T03-load-skill-bundle-with-module-allowlist.md` (added)
- `docs/tasks/2026-09-05-001/T04-inject-plan-skill-into-planner.md` (added)
- `docs/tasks/2026-09-05-001/T05-inject-work-skill-into-executor.md` (added)
- `docs/tasks/2026-09-05-001/T06-schema-driven-tasks-phase-validation.md` (added)
- `docs/tasks/2026-09-05-001/T07-render-final-plan-artifact.md` (added)
- `docs/tasks/2026-09-05-001/T08-write-task-artifacts-one-ac-per-task.md` (added)
- `docs/tasks/2026-09-05-001/T09-register-plan-and-task-indexes.md` (added)
- `docs/tasks/2026-09-05-001/T10-executor-prompt-is-test-first.md` (added)
- `docs/tasks/2026-09-05-001/T11-generate-uses-prompt-library.md` (added)
- `docs/tasks/2026-09-05-001/T12-forward-only-task-status-updates.md` (added)
- `docs/tasks/2026-09-05-001/T13-one-work-report-no-phase-dumps.md` (added)
- `docs/tasks/2026-09-05-001/T14-offline-cli-pipeline-proof.md` (added)
- `docs/tasks/2026-09-05-001/T15-document-skill-driven-pipeline.md` (added)
- `docs/tasks/2026-09-05-001/index.md` (added)

## Commit Messages

### `961f6ed`

```text
Run plan and work skills in the agent pipeline

The agent's planner prompt was a weaker copy of the procedure this repo
already documents in .agents/skills, and the prompt library's generator
builder was dead code: the loop sent a generic system turn instead, so no
stage followed a documented workflow. Both stages now execute the skills
themselves, so editing a skill changes the agent's behaviour.

The skills assume a human approving each phase, so a run pins
interactionMode to autopilot, forbids asking questions, always runs the
Tasks phase, and skips the phase dumps (.scope, .research, .design,
.work) that exist only as review material. Durable outputs are the plan
document, the task files, the generated app and one work report.
Injection is append-only, so the forced write_plan contract hardened in
4e6c6a6 still wins and is restated after the skill body.

Baseline was broken at session start — agent:typecheck failed on two
unused imports in generalization.test.ts — and was fixed first so every
later change was measured against a clean gate. agent:test 274/274.

Plan 2026-09-05-001 records the change: 15 tasks, 12 landed green,
T11/T14/T15 open (prompt-library assertion, offline end-to-end proof,
READMEs).

Used: -plan, -work

[AGENT: pi]
```

### `86e34d2`

```text
Record plan 2026-09-05-001 and its 15 tasks

The skill-driven pipeline was built before it was written down, so the
plan exists to make the change reviewable and resumable: it records the
three-layer design (load, compose, materialize), the alternatives that
were rejected and why — letting the model author the artifacts itself, a
second LLM call for the plan prose, dumping whole skills into every
prompt — and the risk that a task can read `completed` before the app
compiles.

Tasks are sliced one per Acceptance Criterion with one test each. Status
is recorded honestly: 12 are `completed` because each has a green named
test in the suite, and T11, T14 and T15 stay `not-started` because their
behaviour exists in the tree without an assertion pinning it. `/plan` ran
in autopilot, so Scope/Research/Design were reasoned inline and wrote
nothing: the plan frontmatter says so with `phases-inlined` and carries
the plan-id in their id fields instead of pointing at missing files.

`/work 2026-09-05-001` resumes at T11.

Used: -plan

[AGENT: pi]
```

## Verification

- `npm run agent:typecheck` → exit 0 (broken at session start: two `TS6133` unused imports).
- `npm run agent:test` → **274 passed / 274** (baseline was 221/222 with 1 failing file).
- `/plan` orchestrator gates for 2026-09-05-001: schema fields, all 8 Deep-tier sections, cross-phase
  plan-id coherence, `interactionMode: autopilot` coherence, tier/complexity coherence, learnings
  referenced, repository-relative paths only, phase-dump directories untouched — 8/8 pass.
- Task-artifact validation for all 15 files: required frontmatter, exactly one `files.test` entry,
  singular Acceptance Criterion, Red → Green → Refactor step order, status/checkbox agreement,
  dependency ordering (no task depends on a later one), index checklist coherence — pass.
- Working tree clean after both commits; branch `work/cli-agentic-code-generator`, not pushed.

## Next Steps

- Run `/work 2026-09-05-001` to close the three open tasks:
  - `2026-09-05-001-T11` — assert that `generate()` drives the loop from `buildGeneratorPrompt` and
    reports `writtenFiles` + the task artifact it executed (behaviour landed, assertion did not).
  - `2026-09-05-001-T14` — offline end-to-end proof of one `run()`: plan skill → task artifacts →
    work skill → recorded statuses.
  - `2026-09-05-001-T15` — document the wiring, `--artifacts-dir`, the `.agents/skills` default, and
    what a run deliberately skips (`agent/README.md`, `agent/skills/README.md`).
- Follow-on, not planned here: wire `validate()` + the repair loop into `orchestrate()` so task
  `status` is gated on a real typecheck/test pass instead of a per-task loop outcome. Today's plan
  records this as a risk row and a learning gap.
- First keyed run should re-measure cost: the planner carries roughly 6.6k tokens of `plan` skill and
  each executor iteration roughly 5.7k tokens of `work` skill. `agent/README.md`'s existing cost
  figures predate this change.
- Learnings to capture via `/learn` (not written this session — Work does not own `docs/learn/`):
  non-interactive injection of interactive skills (what must be overridden), skill-size budgeting per
  prompt type, and whether status should be gated by the validator instead of the loop.
- Push is manual: both commits are local on `work/cli-agentic-code-generator`.
