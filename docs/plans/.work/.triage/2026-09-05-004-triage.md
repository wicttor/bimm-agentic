---
triage-id: 2026-09-05-004-triage
work-id: 2026-09-05-007
work-branch: work/skill-block-sandbox-boundary
work-branch-base: work/agent-cli-state-and-sandbox-boundary
work-branch-state: created
input-shape: ad-hoc
interactionMode: autopilot
status: complete
timestamp: 2026-09-05T20:15:00Z
---

# Work Manifest — 2026-09-05-004-triage (ad-hoc: work-skill injection vs sandbox)

## Work Input Artifact (as taken)

```yaml
type: work-input
source: user
status: complete
interactionMode: autopilot
plan-id: null
task-file: null
ad-hoc: >
  The custom agent logged { "code": "not_found",
  "message": "no directory at \"docs/tasks\" inside the output directory." } during a run.
  Diagnose and fix it; chosen scope: prompt boundary only.
goals:
  - the executor call stops issuing tool calls the sandbox is obliged to refuse
constraints:
  - no change to the sandbox itself (agent/src/tools/fs.ts stays as task T05 established it)
  - no second sandbox root, no docs/ copy into the output directory
  - .agents/skills/work/SKILL.md is not edited — the injection layer owns the adaptation
references:
  - agent/src/skill-prompts.ts
  - agent/src/prompts/generator.ts
  - agent/src/scaffold.ts (DEFAULT_EXCLUDE)
  - agent/src/tools/fs.ts (listFilesTool, line 153)
```

## Resolution (ad-hoc shape)

- **Classification (Step 1):** the input matched neither a `YYYY-MM-DD-NNN` plan-id nor a
  `docs/tasks/<plan-id>/T<NN>-*.md` path → `input-shape: ad-hoc`.
- **work-id (Step 2c.1):** highest existing `docs/tasks/2026-09-05-*` folder is `006` → allocated
  `2026-09-05-007`. Folder and index created at `docs/tasks/2026-09-05-007/index.md`.
- **Problem frame:** the per-task executor call obeys the verbatim-injected `work` skill and calls
  `list_files("docs/tasks")` (Pre-Flight Check / Self-Healing `mkdir -p`), which its sandbox must
  refuse — `docs` is excluded from the scaffold (`agent/src/scaffold.ts:33`) and the tools are confined
  to `ctx.outDir` (`agent/src/tools/fs.ts`).
- **Intended behavior:** the injected block states which sections of the skill are the harness's, so the
  model never attempts them, and the boundary is authored in exactly one place.
- **Success criteria → ACs:** two criteria, one task each. Neither exceeds 3 files, so no split needed.
  `tier: fast` (2 tasks).
- **Deliberate non-scope:** the `not_found` message text, `list_files` semantics for missing
  directories, and a second resolved root were each considered and rejected by the user's scope choice.
  `.agents/skills/work/SKILL.md` is left alone: its Pre-Flight Check is correct for a human-orchestrated
  session with repository access.

## Tasks

| task-id | title | unit | AC | files | priority | deps | status | ready |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-05-007-T01 | Void the harness-owned sections of the injected work skill | U1 | `buildSkillBlock` for `work` voids `Pre-Flight Check`, `Self-Healing`/`mkdir -p`, `Index Registration`, `Work Report` and `Work Branch`/`git` by name with the sandbox-confinement reason, and its durable-output line names no `docs/` path — `plan` unaffected | modify: `agent/src/skill-prompts.ts` · test: `agent/tests/skill-prompts.test.ts` (new) | P0 | — | not-started | **true** |
| 2026-09-05-007-T02 | Say the sandbox boundary once in the assembled executor prompt | U2 | the `buildGeneratorPrompt` system prompt states the boundary exactly once, with the `executorSkillRules()` duplicate deleted from `agent/src/prompts/generator.ts` | modify: `agent/src/prompts/generator.ts` · test: `agent/tests/prompts.test.ts` | P1 | T01 | not-started | false (T01 not completed) |

- **ready-tasks:** `[2026-09-05-007-T01, 2026-09-05-007-T02]` (both `not-started`; T02 gated on T01 by dependency order)
- **already-complete-tasks:** `[]`
- **dependency-warning:** null (ad-hoc shape)
- **work-state:** ready

## Work Branch (Step 2d)

- Tree verified clean at entry (`git status --porcelain` → 0 lines).
- Slug from the resolved subject → `work/skill-block-sandbox-boundary` (31 chars, kebab-case).
- **Deviation, recorded deliberately.** The Autopilot rule is "create from the default branch". The
  default branch is `main`, and `agent/src/skill-prompts.ts`, `agent/src/prompts/generator.ts` and
  `agent/tests/prompts.test.ts` **do not exist there** — `main` is 35 commits behind
  `work/agent-cli-state-and-sandbox-boundary`, the branch that carries the skill-injection pipeline.
  Branching from `main` would have produced a branch with nothing to fix and an Execute phase that
  fails on a missing module. The branch was therefore created from the current HEAD, which is itself a
  `work/*` branch. Consequence to flag for the user: this run's commits stack on unmerged work and must
  be merged/rebased together with `work/agent-cli-state-and-sandbox-boundary`.
- `work-branch-state: created`, checked out and verified before any artifact in this run was written.

## Related Learnings (per task)

- `2026-09-05-007-T01`:
  - `docs/learn/gotcha/loose-casts-in-agent-tests-break-typecheck-wiring.md` — RECOMMENDED: agent tests
    are typechecked; narrow the `SkillBundle`/tool-result unions in the new test instead of casting.
  - `docs/learn/pattern/test-config-isolation-behaviorally-not-regex.md` — RECOMMENDED (and partly
    refuted here): the house pattern is to prove behaviour with real tooling rather than regex-match
    config text. Prompt rendering *is* the behaviour under test, so text assertions are the right
    instrument — but they are asserted on short stable sentinels, not whole sentences, to keep
    rephrasing from becoming false failure.
- `2026-09-05-007-T02`:
  - `docs/learn/gotcha/ts-block-comment-glob-star-slash-terminates-early.md` — DIRECT: the new comment
    wording in `skill-prompts.ts`/`generator.ts` must not embed `**/*` globs inside `/* */` blocks.
- `2026-09-05-007-T01`, `T02` both:
  - `docs/learn/decision/agent-tsconfig-standalone-no-extends-app-root.md` — DIRECT: run
    `npm run agent:typecheck`, not the root one, for these files.

## Learning Gaps

- `gap_name: "prompt-injection boundary rules have no test file of their own"`
  `domain: testing` — `buildSkillBlock` is asserted indirectly from `prompts.test.ts`,
  `generator.test.ts` and `pipeline-skills.test.ts`; nothing tests it directly, which is how a
  durable-output line contradicting the tool confinement survived.
  `relevance: T01 creates the missing direct test surface.`
  `suggested_action: Document post-implementation (candidate for /learn: gotcha — verbatim skill bodies injected into sandboxed calls need their impossible sections voided explicitly).`
- `gap_name: "no index of which skill sections are harness-owned in the agent pipeline"`
  `domain: agent-runtime` — the harness/skill division is stated in prose comments in three modules.
  `relevance: a single list would let a skill edit be checked against it.`
  `suggested_action: Research external resource` (compare with how the `plan`/`work` skills declare
  preconditions) — out of scope for this run.
