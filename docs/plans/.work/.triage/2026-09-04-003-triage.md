---
triage-id: 2026-09-04-003-triage
work-id: 2026-09-04-001
work-branch: work/cli-agentic-code-generator
work-branch-base: main
work-branch-state: already-on
input-shape: task-file
interactionMode: smart
status: complete
timestamp: 2026-09-04T23:19:00Z
work-state: ready
---

# Work Manifest — 2026-09-04-001-T03

## Input Resolution

- **Shape:** task-file — `docs/tasks/2026-09-04-001/T03-llm-adapter.md`
- **work-id:** `2026-09-04-001` (inherited from the task file's folder)
- **Upstream dependencies:** `2026-09-04-001-T02` — `docs/tasks/2026-09-04-001/index.md` row is
  `- [x]` and the T02 run closed with a passed gate (`2026-09-04-002-review`). All dependencies
  satisfied → **no dependency warning** (Triage Step 2b).
- **Scope:** executes **only this one task's** Red → Green → Refactor cycle. Downstream dependents
  (T04, T05, T07, T08 …) are never included.
- **Work branch:** `work/cli-agentic-code-generator` is the current branch, tree is clean
  (`git status --porcelain` empty) → state `already-on`, no branch operation performed. Base `main`.

## Resolved Task List (dependency order)

| task-id | title | unit | priority | deps | status | risk | ready |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-04-001-T03 | Provider-agnostic LLM adapter | U2 | P0 | T02 (completed) | not-started | low | true |

### 2026-09-04-001-T03

- **Acceptance Criterion (one):** `AnthropicProvider` and `OpenAIProvider` each translate internal
  request/response types to and from their wire format against a stubbed `fetch` (no network), and
  both satisfy the same interface as `FakeProvider`, including normalized `usage` token counts and a
  terminal `stopReason`.
- **Files:**
  - create: `agent/src/llm/provider.ts`, `agent/src/llm/anthropic.ts`, `agent/src/llm/openai.ts`,
    `agent/src/llm/fake.ts`
  - modify: *(none)*
  - test (exactly one): `agent/tests/llm-adapters.test.ts`
- **Steps:** Red → Green → Refactor (verbatim from the task file), five test scenarios:
  1. Anthropic request → `tools[].input_schema`, `system` hoisted to top level
  2. OpenAI request → `tools[].function.parameters`, `role: "system"` message
  3. Tool-call response from each wire shape → one internal `toolCall` with parsed JSON arguments
  4. Malformed `arguments` → typed adapter error, not an `undefined` read
  5. Non-2xx 429 → classified retryable, bounded backoff, then a typed failure
- **Scope constraints carried:**
  - `agent/src/index.ts` and `agent/src/config.ts` are **not** modified — CLI provider wiring is
    T10/T11 scope. `index.ts`'s `ProviderLike` seam stays as-is.
  - No new npm dependencies (uses global `fetch`, Node ≥ 20.6); `package.json` untouched.
  - Anthropic/OpenAI endpoint URLs, API-version headers, and backoff budget are defined inside the
    adapter files themselves (they belong to T03's file list; no config change needed).
  - The 429 test must be bounded in wall-clock time — backoff sleeps are injectable/tiny in tests so
    the suite never waits on real timers.

- **ready-tasks:** `[2026-09-04-001-T03]`
- **already-complete-tasks:** `[2026-09-04-001-T01, 2026-09-04-001-T02]` (outside this run's scope;
  carried for the Work Report)

## Related Learnings

Scoped per task from `docs/learn/index.md` (T03 only):

| learning | relevance | why it applies |
| --- | --- | --- |
| `docs/learn/pattern/dry-run-zero-network-proven-by-injection-spies.md` | DIRECT | AC says "against a stubbed `fetch` (no network)" — replace `globalThis.fetch` with a recording stub, restore in `afterEach`, assert the request count |
| `docs/learn/gotcha/loose-casts-in-agent-tests-break-typecheck-wiring.md` | DIRECT | `agent/tests/**` is typechecked; a loose `as` cast in the new test fails `agent:typecheck`, which fails T01's wiring test in a *different* suite |
| `docs/learn/gotcha/ts-block-comment-glob-star-slash-terminates-early.md` | DIRECT | Comments in the new files must not contain a `*/` sequence (e.g. globs) |
| `docs/learn/decision/agent-tsconfig-standalone-no-extends-app-root.md` | DIRECT | New modules compile in the node unit: `ES2022` lib, `types: ["node"]`, no DOM/React globals available |
| `docs/learn/decision/explicit-provider-missing-key-fails-no-fallback.md` | MEDIUM | Provider selection is already resolved upstream; adapters take an explicit key and never auto-switch providers |
| `docs/learn/pattern/test-config-isolation-behaviorally-not-regex.md` | RECOMMENDED | Assert wire shapes by observing the stubbed request body, not by string-matching source |

## Learning Gaps

- **Owned by this task (carried from the plan):** *provider-agnostic function calling* — the task
  file's Notes say to capture it as a `pattern` learning after this task lands. Surfaced again in the
  Work Report's `learnings-to-capture`; Work never writes `docs/learn/` itself.
- No new gap identified at Triage time.

## Confirmation (Smart mode)

No Smart pause trigger fired: input is not ad-hoc (task list is user-authored, not inferred), the
single upstream dependency (T02) is `completed`, the task artifact validates (one AC, one
`files.test`, Red → Green → Refactor steps, repo-relative paths), the branch slug derives from the
plan name, and the repository is already on that branch (no cross-branch creation from a non-default
HEAD). **Auto-proceed to Prepare.**
