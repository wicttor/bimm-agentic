---
triage-id: 2026-09-05-001-triage
work-id: 2026-09-04-001
work-branch: work/cli-agentic-code-generator
work-branch-base: main
work-branch-state: already-on
input-shape: task-file
interactionMode: autopilot
executionMode: null
status: complete
timestamp: 2026-09-05T00:18:26Z
work-state: ready
---

# Work Manifest — 2026-09-04-001-T04

## Input Resolution

- **Shape:** task-file — `docs/tasks/2026-09-04-001/T04-scaffolder-boilerplate-copy.md`
- **work-id:** `2026-09-04-001` (inherited from the task file's folder)
- **Upstream dependencies:** `2026-09-04-001-T02` — frontmatter `status: completed`, index row is
  `- [x]`, closed by gate-passing review `2026-09-04-002-review`. All dependencies satisfied →
  **no dependency warning** (Triage Step 2b). No `dependency-warning` recorded.
- **Scope:** executes **only this one task's** Red → Green → Refactor cycle. Downstream dependents
  (T05, T06, T10, T13) are never included. T03 is parallel-safe with T04 and already `completed`.
- **Work branch:** `work/cli-agentic-code-generator` is the current branch; tree clean
  (`git status --porcelain` → 0 lines) → state `already-on`, no branch operation performed. Base `main`.
  The plan-based branch slug for `2026-09-04-001` matches the existing branch, so re-runs stay coherent.

## Resolved Task List (dependency order)

| task-id | title | unit | priority | deps | status | risk | ready |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-04-001-T04 | Scaffolder: boilerplate copy | U3 | P0 | T02 (completed) | not-started | low | true |

### 2026-09-04-001-T04

- **Acceptance Criterion (one):** Scaffolding copies the app subset (`src/`, `public/`,
  `index.html`, `package.json`, `tsconfig.json`, `vite.config.ts`, `vitest.config.ts`) into the
  output directory, excludes `node_modules/`, `agent/`, `docs/` and any pre-existing output, and
  refuses to overwrite a non-empty output directory unless `--force` is passed.
- **Files:**
  - create: `agent/src/scaffold.ts`
  - modify: *(none)*
  - test (exactly one): `agent/tests/scaffold.test.ts`
- **Steps:** Red → Green → Refactor (verbatim from the task file), three test scenarios:
  1. Fresh: empty out dir → `generated-app/src/types.ts` present, `generated-app/agent/` absent
  2. Clobber guard: out dir already has `src/App.tsx` → refuses; `force` replaces
  3. Read-only guarantee: reference `src/App.tsx` byte-identical after scaffolding
- **Scope constraints carried:**
  - `agent/src/config.ts` and `agent/src/index.ts` are **not** modified — `files.modify` is empty.
    The `--force` CLI flag does not exist in T02's flag list; `force` is exposed as
    `ScaffoldOptions.force` only. Recorded as a Review note for T13 (CLI wiring).
  - Copy subset is **exactly** the AC's list; `vite-env.d.ts` is not added (not in the AC) — noted
    as a possible T06 consideration, not silently fixed here.
  - No new npm dependencies (uses `node:fs`, `node:path`, `node:os`); `package.json` untouched.
  - Tests write only under `os.tmpdir()` fixtures — nothing lands in the repository working tree.
  - Reference `src/` must never be written to (hard invariant across all units).

- **ready-tasks:** `[2026-09-04-001-T04]`
- **already-complete-tasks:** `[2026-09-04-001-T01, 2026-09-04-001-T02, 2026-09-04-001-T03]` (carried,
  never re-opened)

## Task Artifact Validation (Step 3)

| invariant | result |
| --- | --- |
| exactly one `## Acceptance Criterion` | pass |
| exactly one `files.test` entry | pass (`agent/tests/scaffold.test.ts`) |
| Red → Green → Refactor step order | pass (steps 1/2/3) |
| repo-relative, backtick-formatted paths | pass |
| dependencies reference existing task ids | pass (`T02` exists and is `completed`) |

## Related Learnings (per task)

Matched against `docs/learn/index.md` (8 entries).

| learning | applicability | relevance | applied to |
| --- | --- | --- | --- |
| `gotcha/ts-block-comment-glob-star-slash-terminates-early.md` | DIRECT | HIGH | T04 — never write a `/** */` comment containing `agent/**/*.test.ts` in the test or module |
| `gotcha/loose-casts-in-agent-tests-break-typecheck-wiring.md` | RECOMMENDED | HIGH | T04 — agent tests are typechecked: narrow the `ScaffoldResult` discriminated union, no `as` casts |
| `pattern/test-config-isolation-behaviorally-not-regex.md` | DIRECT | MEDIUM | T04 — assert copy/exclusion outcomes from the filesystem, not by regex-matching `scaffold.ts` source |
| `decision/agent-tsconfig-standalone-no-extends-app-root.md` | DIRECT | MEDIUM | T04 — new module stays inside the node compilation unit (`node:*` imports only, DOM-free) |
| `workflow/spawn-real-cli-entry-in-tests.md` | DIRECT | LOW | T04 — not needed: the AC is on the `scaffold()` unit, not the CLI entry; no process spawn |
| `pattern/dry-run-zero-network-proven-by-injection-spies.md` | DIRECT | LOW | T04 — no network in scope |

## Learning Gaps

- **No established pattern for filesystem-mutating agent tests in this repo** (temp-fixture
  creation/cleanup, byte-identity assertions, symlink-vs-deep-copy checks). T04 will be the first
  task to touch disk; if a reusable fixture helper emerges, it should be captured by `/learn`.
- **No learning yet on clobber-guard UX** (how the refusal message names the offending path and the
  escape hatch) — T04's decision becomes the reference for T05's sandbox errors.
