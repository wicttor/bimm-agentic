---
execute-id: 2026-09-05-005-execute
prepare-id: 2026-09-05-004-prepare
triage-id: 2026-09-05-004-triage
work-id: 2026-09-05-007
work-branch: work/skill-block-sandbox-boundary
input-shape: ad-hoc
interactionMode: autopilot
executionMode: serial
status: complete
timestamp: 2026-09-05T20:20:40Z
---

# Execution Log — 2026-09-05-005-execute (2026-09-05-007)

Commands run per task, per the Execution Plan: `npx vitest run --config agent/vitest.config.ts <path>`
for the task's AC test, `npm run agent:test` and `npm run agent:typecheck` for the regression gate.

## Results

### 2026-09-05-007-T01 — Void the harness-owned sections of the injected work skill → `completed`

- **Red** (`2026-09-05T20:18:30Z`) — created `agent/tests/skill-prompts.test.ts` complete before its
  first run, against a temp skills dir the test writes itself (a `work/SKILL.md` carrying real
  `Pre-Flight Check`, `Self-Healing`, `Index Registration`, `Work Report`, `Work Branch` headings, plus
  `modules/execute.md`, and a `plan/SKILL.md`). Result: **5 failed | 3 passed (8)**. The failure reason
  is content, not plumbing — the render-premise test passed, and the visible diff showed the live
  offending line:
  `4. The durable outputs of this run are: \`the files named by the current task\`, \`the task's status line in docs/tasks/<plan-id>/index.md\`.`
  The 3 passes are the guard assertions (`plan` block unchanged, block renders at all).
- **Green** (`2026-09-05T20:19:03Z`) — in `agent/src/skill-prompts.ts`: added
  `WORK_HARNESS_OWNED_SECTIONS` and a `harnessOwned` field on `autopilotOverrides`, rendered only when
  the list is non-empty (so the `plan` path is untouched), keyed off the existing `isPlan` branch; and
  reduced the `work` `durableOutputs` to `["the files named by the current task"]`. 8/8 green; whole
  suite **296 passed (19 files)** vs a 288 baseline, typecheck clean → no regression.
- **Refactor** — verified the rule is a single string in the overrides array (no second rendering path),
  `SKILL_BLOCK_MAX_BYTES` and `omitted` reporting untouched, `droppedArtifacts` for both skills
  untouched. Re-rendered against the **real** `.agents/skills` to read the prose in situ — override 4
  now names all four harness-owned sections and states the `not_found` / `path_escape` / `git` refusals
  as correct. Suite re-run clean.
- Retries used: 0. Index checkbox flipped: yes. Commit `e21d2ee`.

### 2026-09-05-007-T02 — Say the sandbox boundary once in the assembled executor prompt → `completed`

- **Red** (`2026-09-05T20:20:05Z`) — added one case to the `buildGeneratorPrompt` describe block in
  `agent/tests/prompts.test.ts`, building from the default (real) `.agents/skills` dir. Result:
  **1 failed | 66 passed (67)**, failing on
  `expect(prompt.system).not.toContain("planning artifact under")` — i.e. precisely the duplicate this
  task deletes, not on the T01 rule's presence (those assertions already pass, correctly: T01 shipped
  them).
- **Green** (`2026-09-05T20:20:27Z`) — deleted the `docs/` rule from `executorSkillRules()` in
  `agent/src/prompts/generator.ts`. Targeted files: **89 passed (3 files)**; whole suite
  **297 passed (19 files)**, typecheck clean.
- **Refactor** — collapsed the two stacked one-line comments above `executorSkillRules()` into one block
  comment recording why the rule lives in `skill-prompts.ts` alone. Confirmed no other test pinned the
  deleted string (a repo-wide grep for `planning artifact` returns only the new negative assertion).
  Assertion is on the **count** of the sentinel `harness performs`, not on `toContain`, so a re-added
  copy fails this test.
- Retries used: 0. Index checkbox flipped: yes. Commit `6c6e721`.

## Aggregators

```yaml
total: 2
completed: 2
blocked: 0
skipped: 0
in-progress-left: 0
```

## Regression Detection

```yaml
baseline-state: green          # 18 files / 288 tests + clean agent:typecheck, taken before any Red
counts: { baseline: 288, after-T01: 296, after-T02: 297 }
new-regressions: []
snapshot-now-passing: []       # empty snapshot: the baseline was green
```

Test count moved only upward, and the +9 are the two new AC tests (8 in `skill-prompts.test.ts`, 1 in
`prompts.test.ts`). No existing assertion changed its result.

## Final Index State

```yaml
checklist-matches-task-files: true      # both rows - [x], both files status: completed
uncompleted-rows: []
```

## Notes for Review

- The `git` commit-per-task boundary follows the convention established by plan `2026-09-05-003`.
- Nothing outside `agent/src/skill-prompts.ts`, `agent/src/prompts/generator.ts` and the two test files
  was modified by the implementation; the rest of the diff is this run's own `docs/` artifacts.
- T01's test asserts on prompt text, which the learnings entry
  `docs/learn/pattern/test-config-isolation-behaviorally-not-regex.md` would normally discourage. The
  exception is deliberate and is argued in the new test's header comment: rendered prompt text **is**
  the behaviour here, and the failure mode being prevented is invisible-in-output prose drift.
