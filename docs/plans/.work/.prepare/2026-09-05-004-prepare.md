---
prepare-id: 2026-09-05-004-prepare
triage-id: 2026-09-05-004-triage
work-id: 2026-09-05-007
work-branch: work/skill-block-sandbox-boundary
input-shape: ad-hoc
interactionMode: autopilot
executionMode: serial
status: complete
timestamp: 2026-09-05T20:17:30Z
---

# Execution Plan — 2026-09-05-004-prepare (2026-09-05-007)

## Test Environment (Step 1)

- **Runner detection:** `package.json` + `package-lock.json` at the repository root → Node.js on npm
  (`packageManager` unset, no `pnpm-lock.yaml`). `scripts.test` is `vitest run`.
- **Scope of this work is the `agent` sub-project** — every file in `files.create`/`files.modify` lives
  under `agent/`, so the governing commands are the two npm scripts that target it, not the root app
  project:
  - typecheck: `npm run agent:typecheck` (`tsc --noEmit -p agent/tsconfig.json`)
  - tests: `npm run agent:test` (`vitest run --config agent/vitest.config.ts`), single file:
    `npx vitest run --config agent/vitest.config.ts agent/tests/<name>.test.ts`
  - Learning `docs/learn/decision/agent-tsconfig-standalone-no-extends-app-root.md` is why these are
    separate from the root project and must not be collapsed.
- **Baseline run (before any Red step):**
  - `npm run agent:typecheck` → exit 0, no diagnostics.
  - `npm run agent:test` → **Test Files 18 passed (18) · Tests 288 passed (288)**, duration ~9.4s.
  - `baseline.state: green`, so `known-failing-snapshot` is empty and **any** new failure during
    Execute is a regression (`task-execution-rules.md`: "When `baseline: green`, the snapshot is empty —
    any failure is a regression").
- **`files.test` resolution:** `agent/tests/prompts.test.ts` exists ✔; `agent/tests/skill-prompts.test.ts`
  does not exist yet and is created by T01's Red step — a not-y-existing test path is valid for Red. Its
  directory (`agent/tests/`) exists, so nothing is unresolvable.

## Execution List (Step 2)

Dependency order preserved from the Work Manifest, not re-sorted:

```
serial: [ 2026-09-05-007-T01, 2026-09-05-007-T02 ]
```

T02 declares `dependencies: [2026-09-05-007-T01]`, so the list is a single chain. Wave grouping is not
needed (and would be degenerate: Wave 0 = T01, Wave 1 = T02). `upstream-skipped: false` — not a
task-file shape run.

## Execution Mode (Step 3)

- **Default computed:** no `P0`-with-HIGH-risk task; 2 tasks; a strict dependency chain means zero
  independent waves → `parallel` is not available; no `interactionMode: detailed` → `inline` is not
  forced. → **`serial`**, selected automatically (Autopilot never asks).
- **Risk floor:** both tasks are prompt-text changes covered by a green suite; nothing HIGH-risk, so
  nothing forces `inline`. The suite still gates each task, and `serial` stops on a `blocked` task and
  surfaces it.
- `single-task-pause: false` (serial: no per-task pause; stop only on `blocked`).

## Applicable Gates (Step 4, read-only from task-execution-rules.md)

- `applicable-gates: [red, green, refactor, complete]`.
- **Red** for T01/T02 needs care that is recorded here rather than improvised in Execute: both tests
  assert on rendered prompt text, so a Red must fail on an **assertion about content**, not on a missing
  module or an import error. T01's test file is new, so it must be written complete before its first
  run, and `buildSkillBlock` must already be importable and already render a `work` block (it does —
  the skill exists at `.agents/skills/work/SKILL.md`).
- **Green** additionally requires the whole agent suite to stay green, not just the task's file, since
  `agent/tests/prompts.test.ts`, `generator.test.ts` and `pipeline-skills.test.ts` all assert on the
  rendered skill block and can be broken by a rewording.
- **`retry-limit: 2`** per task to reach Green; on exhaustion → `blocked` with the failing-assertion
  snapshot, index checkbox stays `- [ ]` with `— blocked: <reason>`.
- **Policy:** resume `completed` tasks never re-open, checkboxes tick forward only; skip requires
  explicit user confirmation (Autopilot does not auto-skip); complete transition flips frontmatter
  `status`, the task file's `## Acceptance Criteria` checkbox, and the index row.
- **Regression policy:** `baseline: green` → any new failure is a regression and fails the Green/Refactor
  gate.

## Guardrails carried from Triage

- Do not edit `.agents/skills/work/SKILL.md`, `agent/src/tools/fs.ts`, or
  `agent/src/scaffold.ts` — the sandbox and the skill are both correct; only the injection boundary
  changes.
- Do not remove the `droppedArtifacts` paths for either skill: `agent/tests/prompts.test.ts` (~578–592)
  pins `docs/plans/.work/.triage/` and `docs/plans/.work/.review/`.
- No `as` casts in the new test (learnings gate); no `**/*` glob inside a `/* */` comment header.

- `work-state: ready` (echoed from the Work Manifest)
