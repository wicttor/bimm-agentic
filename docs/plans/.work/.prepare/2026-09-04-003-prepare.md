---
prepare-id: 2026-09-04-003-prepare
triage-id: 2026-09-04-003-triage
work-id: 2026-09-04-001
input-shape: task-file
interactionMode: smart
executionMode: inline
work-branch: work/cli-agentic-code-generator
status: complete
timestamp: 2026-09-04T23:20:00Z
work-state: ready
---

# Execution Plan — 2026-09-04-001-T03

## Inherited State

- **Manifest:** `2026-09-04-003-triage` — one ready task (`2026-09-04-001-T03`), no dependency
  warning, no `work-state: nothing-ready`.
- **Branch coherence (orchestrator gate #5):** `git branch --show-current` =
  `work/cli-agentic-code-generator` = manifest `work-branch`. ✅
- **Pre-Execute tree state:** clean except the Triage artifact just written
  (`git status --porcelain` → `?? docs/plans/.work/.triage/2026-09-04-003-triage.md`).

## Test Execution Environment

- **Runner:** Node.js + vitest. Lockfile `package-lock.json` → **packageManager: npm**.
- **Agent test command (per task):**
  `npx vitest run agent/tests/llm-adapters.test.ts --config agent/vitest.config.ts`
- **Whole-scope commands:** `npm run agent:test` (node env, agent project) · `npm test` (jsdom, app
  project) · `npm run agent:typecheck` · `npm run typecheck`
- **Node:** v26.8.1 → global `fetch` available (task requires ≥ 20.6), no HTTP client dependency.

### Baseline: green

| Command | Result |
| --- | --- |
| `npm run agent:test` | 2 files, **18/18 passed** (`config.test.ts` 12, `wiring.test.ts` 6) |
| `npm test` (app) | 1 file, **2/2 passed** (`src/__tests__/Example.test.tsx`) |
| `npm run agent:typecheck` | exit 0, no diagnostics |

`baseline: green` → the regression snapshot is **empty**: any failure later is a regression. No
Prepare Smart pause trigger (`baseline: red` not applicable).

### Test path resolution

`agent/tests/llm-adapters.test.ts` does not exist yet — valid for a Red step (Execute writes it).
Its directory `agent/tests/` exists. The four `files.create` paths live under `agent/src/llm/`, a
new directory; `agent/tsconfig.json` uses `include: ["**/*"]`, so it is picked up automatically.

## Execution List

Flat, dependency order preserved (single task; no re-sorting):

1. `2026-09-04-001-T03` — Provider-agnostic LLM adapter (U2, P0)

## Execution Mode

Computed per [execution-mode-selection.md](../../../.agents/skills/work/references/execution-mode-selection.md)
Selection Algorithm (authoritative, not the prepare-module table shorthand):

- Step 1: T03 priority = **P0**, risk flag = low.
- Step 2: `default_mode = inline` — "any P0 task present" fires.
- Step 3: no HIGH-risk task → no additional risk-floor override beyond the P0 default.
- Step 4: `interactionMode: smart` → auto-select, no question.
- **Recorded:** `executionMode: inline`, `single-task-pause: true`.
- Practical effect for a one-task list: the between-tasks re-confirm never fires (no next task), and
  the Red step is purely additive (one new test file + one new source directory) with a green
  baseline and a clean tree, so no Smart-mode pause trigger applies.

## Applicable Gates (read-only, authoritative in task-execution-rules.md)

| Gate | Criteria applied |
| --- | --- |
| Red | Test exists at `files.test[0]` and **fails for the right reason** — an assertion matching the AC fires, *not* a setup/import/compile error |
| Green | The single AC test passes **and** no new failure beyond the empty baseline snapshot (agent suite + app suite + `agent:typecheck`) |
| Refactor | Cleanup only; AC test stays green, no behavior change, no regression |
| Complete | frontmatter `status: completed`; `## Acceptance Criteria` checkbox → `- [x]`; index row `- [ ]` → `- [x]` (forward only) |

- **retry-limit: 2** Green attempts from a confirmed Red → then `blocked` with the failing-assertion
  snapshot and the gate that failed recorded in the task file and the index note.
- **Resume policy:** `completed` tasks never re-open; `blocked`/`skipped` carried, not auto-retried.
- **Skip:** requires explicit user confirmation — never auto-skip.
- **Mid-task interruption:** frontmatter `status: in-progress`, checkbox untouched.

## Notes for Execute

- **Red-gate discipline:** `agent/tsconfig` resolves `.ts` imports with
  `allowImportingTsExtensions`; imports must use the `./llm/provider.ts` style (as `index.ts` does
  with `./config.ts`). A test importing four nonexistent modules fails at *collection*, which is the
  wrong reason — so Red is established in two moves: (1) scaffolding-free assertion failure against
  stub `fetch` once the type-only surface exists, or (2) confirm the assertion-level failure before
  Green closes. Logged explicitly in the Execution Log.
- **Wire-format constants** (endpoint URLs, `anthropic-version` / OpenAI path, backoff budget) are
  defined inside T03's own files — no edits to `config.ts`/`index.ts`/**`package.json`** (Review's
  scope-creep gate compares the diff against the four `files.create` paths + the one test file).
- **Typestrict constraints:** `noUncheckedIndexedAccess`, `noUnusedLocals`, `noUnusedParameters` —
  index access needs guards; no loose `as` casts in the test file (the loose-casts learning; a test
  type error breaks T01's wiring suite).
- **No real timers** in the 429 backoff test — backoff must be injectable so the suite stays fast
  and deterministic.
- **Comments:** never embed a `*/` sequence inside a block comment (ts-block-comment glob learning).

## Confirmation (Smart mode)

No Prepare Smart pause trigger: baseline is green, `executionMode` is `inline` (not `parallel`, so
the >5-task concurrency trigger cannot fire), and no HIGH-risk task lacks inline treatment.
**Auto-proceed to Execute.**
