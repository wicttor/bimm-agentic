---
prepare-id: 2026-09-04-002-prepare
triage-id: 2026-09-04-002-triage
work-id: 2026-09-04-001
work-branch: work/cli-agentic-code-generator
input-shape: task-file
interactionMode: smart
executionMode: inline
status: complete
timestamp: 2026-09-04T22:55:00Z
work-state: ready
---

# Execution Plan — 2026-09-04-001-T02

## Test Environment (Prepare Step 1)

- **Runner:** Node.js (npm) — `package.json` + `package-lock.json`; `scripts.test = "vitest run"`.
- **packageManager:** `npm` (only `package-lock.json`; no `pnpm-lock.yaml`).
- **Test command:** `npx vitest run --config agent/vitest.config.ts <test-path>` for per-task agent
  tests (equivalent to `npm run agent:test` scoped); `npm test` for the app suite.
- **Typecheck commands:** `npm run typecheck` (app) and `npm run agent:typecheck` (agent).

### Baseline (established on branch `work/cli-agentic-code-generator`)

| Check | Result |
| --- | --- |
| `npx vitest run` (app, jsdom) | **green** — 1 file, 2 tests passed |
| `npm run agent:test` (agent, node) | **green** — 1 file, 6 tests passed (`wiring.test.ts`) |
| `npm run typecheck` | **green** — exit 0 |
| `npm run agent:typecheck` | **green** — exit 0 |

**baseline state: `green`** — known-failing snapshot: `[]`. Any new failure during Execute is a regression.

`files.test` = `agent/tests/config.test.ts` — directory exists; the file itself does not yet exist
(valid for a not-yet-written Red test; Prepare creates nothing).

## Execution List (Prepare Step 2)

- **input-shape: task-file, single task.** `ready-tasks: [2026-09-04-001-T02]`; no downstream dependents.
- Flat list: `[ 2026-09-04-001-T02 ]`.
- `upstream-skipped: false` (T01 dependency is `completed`).

## Execution Mode (Prepare Step 3, authoritative in execution-mode-selection.md)

- Selection algorithm: task priority is **P0** → **`inline`**.
- Single-task list → the `inline` between-task pause is a no-op; `single-task-pause: true`.
- Risk floor: task risk is `low` (deterministic, offline, no network in tests) — no conflict.

## Applicable Gates (Prepare Step 4, read-only from task-execution-rules.md)

| Gate | Criteria applied by Execute |
| --- | --- |
| **Red** | `agent/tests/config.test.ts` exists and **fails for the right reason** — the task's own Red step states the expected failure mode is "CLI/config don't exist yet" (module-not-found / spawn failure on missing entry), recorded in the Execution Log. |
| **Green** | The AC test passes with **no new regression** beyond the green baseline (app suite, agent suite, both typechecks still exit 0). |
| **Refactor** | Cleanup keeps the AC test green, no regression, no behavior change. |
| **Complete** | frontmatter `status → completed`; AC checkbox `- [x]`; index row `- [ ] → - [x]`. |

- **retry-limit:** 2 (task-scoped Green attempts).
- **resume:** completed skipped; blocked/skipped carried; in-progress re-enters; not-started runs.
- **blocked:** green-not-reached-within-retry-limit → `status: blocked` + reason + assertion snapshot; checkbox stays `- [ ]`.
- **skip:** explicit user confirmation only → `status: skipped` + reason; checkbox stays `- [ ]`.
- **regression:** green baseline (snapshot empty) → any failure is a regression.

## Scope Constraints (from Triage, enforced by Execute)

- Create **only**: `agent/src/index.ts`, `agent/src/config.ts`, `.env.example`, `specs/car-inventory.md`, `agent/tests/config.test.ts`. Modify nothing else (no `package.json`, no `agent/src/llm/*`).
- Dry-run test scenario uses a **test-local fake provider** injected via `run(argv, deps)` — T03 owns `FakeProvider`.

## Confirmation (Smart mode)

No Prepare Smart trigger fired: baseline is green (not red), single-task list (no parallel > 5), no
HIGH-risk risk-floor conflict. **Auto-proceed to Execute.**
