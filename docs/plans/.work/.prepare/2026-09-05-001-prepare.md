---
prepare-id: 2026-09-05-001-prepare
triage-id: 2026-09-05-001-triage
work-id: 2026-09-04-001
task-id: 2026-09-04-001-T04
input-shape: task-file
interactionMode: autopilot
executionMode: inline
work-branch: work/cli-agentic-code-generator
work-branch-state: already-on
runner: vitest
packageManager: npm
baseline: green
known-failing-snapshot: []
retry-limit: 2
applicable-gates: [red, green, refactor, complete]
status: complete
timestamp: 2026-09-05T00:20:27Z
---

# Execution Plan — 2026-09-04-001-T04

## Step 0 — Verification (inherited manifest)

`triage-id`, `work-id`, `input-shape`, `interactionMode`, task list and `ready-tasks` all present;
`work-id` matches Triage; `/plan` invariants validated in Triage Step 3. Not `nothing-ready`.

## Step 1 — Test Execution Environment

| item | value |
| --- | --- |
| lockfile | `package-lock.json` → `packageManager: npm` |
| runner | vitest 3 (two projects: root `vitest.config.ts` jsdom for `src/**`, `agent/vitest.config.ts` node for `agent/tests/**`) |
| scoped test command (this task) | `npx vitest run --config agent/vitest.config.ts agent/tests/scaffold.test.ts` |
| full agent suite | `npm run agent:test` |
| agent typecheck | `npm run agent:typecheck` |
| app suite / typecheck | `npm test` / `npm run typecheck` |

### Baseline (established before any file was written) — **green**

| command | result |
| --- | --- |
| `npm run agent:test` | 3 files, **36/36 passed** (wiring 6, config 12, llm-adapters 18) |
| `npm run agent:typecheck` | exit 0 |
| `npm test` | 1 file, **2/2 passed** |
| `npm run typecheck` | exit 0 |

`baseline: green` → the known-failing snapshot is empty → **any** failure during Execute is a
regression (per `task-execution-rules` snapshot policy). No Prepare pause trigger fired.

### `files.test` path resolution

`agent/tests/scaffold.test.ts` does not exist yet — valid for a Red step (its directory
`agent/tests/` exists and holds three passing tests). `agent/src/scaffold.ts` likewise to be created.
No new npm dependency is required: the module uses `node:fs`, `node:path`, `node:os`, and `@types/node`
is already installed (T01). `package.json` is not touched.

## Step 2 — Ordered Execution List

`ready-tasks` from the manifest, dependency order preserved, no re-sorting:

1. `2026-09-04-001-T04` — Scaffolder: boilerplate copy (U3, P0, deps: T02 `completed`)

Flat list (single task) — wave grouping is not applicable; `upstream-skipped: false` (all
dependencies satisfied, so no Triage dependency-warning).

## Step 3 — Execution Mode Selection

| input | decision |
| --- | --- |
| `interactionMode` | `autopilot` → auto-select, never ask |
| task priority / risk | P0, LOW risk (writes only under `os.tmpdir()`; reference tree read-only) |
| default rule | "any `P0` task is in the list" → `inline` |
| risk floor | not applicable (no HIGH-risk task) |
| independent waves | 1 task → `parallel` unavailable |

**`executionMode: inline`** — `single-task-pause: true`, but the pause is suppressed by
`interactionMode: autopilot`. Matches the T01/T02/T03 runs for this `work-id`.

## Step 4 — Per-Task Gates and Policy (read-only, authoritative in `task-execution-rules.md`)

| gate | criteria applied by Execute |
| --- | --- |
| **Red** | `agent/tests/scaffold.test.ts` exists and **fails on assertions**, not on an import/setup/compile error. Requires a compiling stub `scaffold.ts` exporting the final API so the module resolves — a `module-not-found` failure would be an invalid Red. |
| **Green** | that single AC test passes **and** no new failure appears in `npm run agent:test` (36 baseline tests) beyond the empty snapshot; `npm run agent:typecheck` still exits 0. |
| **Refactor** | exclusion list configurable, deep copy (no symlinks), `node_modules`/`agent`/`docs` never copied; test stays green, no regression, no behavior change. |
| **Complete** | frontmatter `status: completed`; task file `## Acceptance Criteria` checkbox → `- [x]`; index row `- [ ]` → `- [x]` (forward only). |

- **retry-limit:** 2 Green attempts from a confirmed Red → then `blocked` with the failing-assertion
  snapshot and the gate name recorded in the task file and the log.
- **resume:** `completed` tasks are never re-opened; `blocked`/`skipped` are carried with reasons and
  not auto-retried; interruption sets `in-progress` without ticking the checkbox.
- **skip:** explicit user confirmation only — never auto-skip (checkbox stays `- [ ]`).
- **regression policy:** `baseline: green` → snapshot empty → any failure is a regression.

## Step 5 — Constraints Carried Into Execute

- Touch only `agent/src/scaffold.ts` (create) and `agent/tests/scaffold.test.ts` (create). Any other
  file edited is a scope-creep finding by construction — notably `agent/src/config.ts` (no `--force`
  flag exists there; `force` is a `ScaffoldOptions` field), `agent/src/index.ts`, `package.json`,
  `.gitignore`, and the reference `src/`.
- Copy subset is exactly `src/`, `public/`, `index.html`, `package.json`, `tsconfig.json`,
  `vite.config.ts`, `vitest.config.ts`.
- All test I/O under `fs.mkdtempSync(os.tmpdir()/scaffold-*)`, removed in `afterEach`.
- No block comment containing a `**/` glob (learned gotcha); narrow the discriminated union instead
  of `as` casts (learned gotcha); assert filesystem outcomes, not source text (learned pattern).

## work-state

`ready` — 1 runnable task.
