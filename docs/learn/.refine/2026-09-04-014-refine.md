---
refine-id: 2026-09-04-014-refine
capture-id: 2026-09-04-014-capture
learn-id: 2026-09-04-014
input-shape: candidate
interactionMode: autopilot
type: workflow
slug: recheck-branch-before-each-commit
dup-status: none
resolution: none
lineage: null
status: complete
timestamp: 2026-09-04T20:20:00-04:00
---

# Refined Entry — recheck-branch-before-each-commit

- **Type validation:** `workflow` is one of the four. Body carries the canonical
  `entry-schema.md` section template for `workflow` in order, with no extra top-level section that
  contradicts it (verified against the two pre-existing `workflow` files' headings).
- **Frontmatter validation:** `slug` kebab-case and unique; `domain` single and non-empty
  (`version-control`) **and present in `tags`** per the coherence rule; `tags` within 2–6;
  `applicability` integers 0–10; `confidence` ∈ enum; `priority` ∈ enum;
  `source` = `{type: candidate, reference: 2026-09-04-003-review#6, extracted_at}`; `created_at` preserved from
  Capture; `updated_at` stamped now.
- **Coherence:** the body H1 kebab-cases to the `slug`; the index projection
  (filename/domain/tags/applicability/summary) is computable from the entry.
- **Duplicate + analog check (dedup-rules.md):**
- `spawn-real-cli-entry-in-tests` — `workflow` (same type) but its domain is `testing` vs this `version-control` and zero shared tags -> insignificant.
- No existing entry has domain `version-control` (store domains so far: typescript, testing, config, llm-integration) -> first entry in a new domain; By Domain gains a bullet.
- No exact slug match.
`dup-status: none` -> genuine new entry; no pause.
- **Candidate provenance:** this candidate is not in the report's numbered Step-5 list of five; it is
the sixth, raised by the Branch Coherence Anomaly section the Review pass appended after the user
chose how to reconcile (`main` = work branch = `585be34`, no history rewrite). Cited with reflog
evidence in the entry's `## Why`.
- **Autopilot:** no exact/analog conflict and no validation failure, so nothing required a user
  decision — the run proceeds to Index without pausing.
