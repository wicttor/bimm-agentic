---
refine-id: 2026-09-04-013-refine
capture-id: 2026-09-04-013-capture
learn-id: 2026-09-04-013
input-shape: candidate
interactionMode: autopilot
type: gotcha
slug: mutation-check-silent-noop-fakes-test-strength
dup-status: none
resolution: none
lineage: null
status: complete
timestamp: 2026-09-04T20:20:00-04:00
---

# Refined Entry — mutation-check-silent-noop-fakes-test-strength

- **Type validation:** `gotcha` is one of the four. Body carries the canonical
  `entry-schema.md` section template for `gotcha` in order, with no extra top-level section that
  contradicts it (verified against the two pre-existing `gotcha` files' headings).
- **Frontmatter validation:** `slug` kebab-case and unique; `domain` single and non-empty
  (`testing`) **and present in `tags`** per the coherence rule; `tags` within 2–6;
  `applicability` integers 0–10; `confidence` ∈ enum; `priority` ∈ enum;
  `source` = `{type: candidate, reference: 2026-09-04-003-review#5, extracted_at}`; `created_at` preserved from
  Capture; `updated_at` stamped now.
- **Coherence:** the body H1 kebab-cases to the `slug`; the index projection
  (filename/domain/tags/applicability/summary) is computable from the entry.
- **Duplicate + analog check (dedup-rules.md):**
- `test-config-isolation-behaviorally-not-regex` — same broad discipline (assert the mechanism, not the text) but `pattern` vs `gotcha` -> insignificant; linked via `related`.
- `loose-casts-in-agent-tests-break-typecheck-wiring` — `gotcha` + `testing` in tags, but its primary `domain` is `typescript` vs this `testing` -> domain-strict-match fails -> insignificant.
- `ts-block-comment-glob-star-slash-terminates-early` — domain `typescript` -> insignificant.
- No exact slug match.
`dup-status: none` -> genuine new entry; no pause.
- Highest `general` score in the batch (9): the no-op-mutation trap is tool-agnostic and applies to any verification run by scripted edit.
- **Autopilot:** no exact/analog conflict and no validation failure, so nothing required a user
  decision — the run proceeds to Index without pausing.
