---
refine-id: 2026-09-04-009-refine
capture-id: 2026-09-04-009-capture
learn-id: 2026-09-04-009
input-shape: candidate
interactionMode: autopilot
type: pattern
slug: provider-agnostic-function-calling-five-wire-divergences
dup-status: none
resolution: none
lineage: null
status: complete
timestamp: 2026-09-04T20:20:00-04:00
---

# Refined Entry — provider-agnostic-function-calling-five-wire-divergences

- **Type validation:** `pattern` is one of the four. Body carries the canonical
  `entry-schema.md` section template for `pattern` in order, with no extra top-level section that
  contradicts it (verified against the two pre-existing `pattern` files' headings).
- **Frontmatter validation:** `slug` kebab-case and unique; `domain` single and non-empty
  (`llm-integration`) **and present in `tags`** per the coherence rule; `tags` within 2–6;
  `applicability` integers 0–10; `confidence` ∈ enum; `priority` ∈ enum;
  `source` = `{type: candidate, reference: 2026-09-04-003-review#1, extracted_at}`; `created_at` preserved from
  Capture; `updated_at` stamped now.
- **Coherence:** the body H1 kebab-cases to the `slug`; the index projection
  (filename/domain/tags/applicability/summary) is computable from the entry.
- **Duplicate + analog check (dedup-rules.md):** store held 8 entries.
- `explicit-provider-missing-key-fails-no-fallback` — related topic (provider selection) but `type: decision` vs this `type: pattern` -> **insignificant** (analog requires same type).
- `dry-run-zero-network-proven-by-injection-spies` — same subject family (offline provider testing) but `pattern`/`testing` domain vs `pattern`/`llm-integration` domain with <=1 shared tag -> **insignificant**.
- No exact slug match (8 existing slugs all distinct). No pair met same-domain + >=2 shared tags + same type, so no semantic comparison was required.
`dup-status: none` -> genuine new entry, no resolution, **no pause required (autopilot compatible).**
- **Gap scope recorded in the entry body:** the plan's `llm-integration` gap is
*"Provider-agnostic function calling + token budgeting … capture after U2/U8"*. This entry closes the
**function-calling (U2)** half only and states up front that **U8 token budgeting is still owed**, so
the KB cannot be misread as gap-complete.
- **Autopilot:** no exact/analog conflict and no validation failure, so nothing required a user
  decision — the run proceeds to Index without pausing.
