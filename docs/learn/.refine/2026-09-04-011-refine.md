---
refine-id: 2026-09-04-011-refine
capture-id: 2026-09-04-011-capture
learn-id: 2026-09-04-011
input-shape: candidate
interactionMode: autopilot
type: gotcha
slug: fetch-stub-reinstall-serves-wrong-payload
dup-status: none
resolution: none
lineage: null
status: complete
timestamp: 2026-09-04T20:20:00-04:00
---

# Refined Entry — fetch-stub-reinstall-serves-wrong-payload

- **Type validation:** `gotcha` is one of the four. Body carries the canonical
  `entry-schema.md` section template for `gotcha` in order, with no extra top-level section that
  contradicts it (verified against the two pre-existing `gotcha` files' headings).
- **Frontmatter validation:** `slug` kebab-case and unique; `domain` single and non-empty
  (`testing`) **and present in `tags`** per the coherence rule; `tags` within 2–6;
  `applicability` integers 0–10; `confidence` ∈ enum; `priority` ∈ enum;
  `source` = `{type: candidate, reference: 2026-09-04-003-review#3, extracted_at}`; `created_at` preserved from
  Capture; `updated_at` stamped now.
- **Coherence:** the body H1 kebab-cases to the `slug`; the index projection
  (filename/domain/tags/applicability/summary) is computable from the entry.
- **Duplicate + analog check (dedup-rules.md):**
- `dry-run-zero-network-proven-by-injection-spies` — the closest relation (same `globalThis.fetch` replacement technique), but `type: pattern` vs this `type: gotcha` -> **insignificant** per the analog rule (different type is not a dedup candidate). Recorded as a `related` link instead: the pattern installs the spy to assert zero calls; this gotcha covers the trap when it is used to serve several payloads.
- `scripted-fake-exhaustion-mimics-adapter-bug` (sibling in this same batch) — same type + domain `testing`, but shared tags are `testing` only (1) -> insignificant.
- No exact slug match.
`dup-status: none` -> genuine new entry; no pause.
- Kept separate from the injection-spies pattern deliberately: same mechanism, opposite failure mode.
- **Autopilot:** no exact/analog conflict and no validation failure, so nothing required a user
  decision — the run proceeds to Index without pausing.
