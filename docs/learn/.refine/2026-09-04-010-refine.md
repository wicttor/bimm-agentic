---
refine-id: 2026-09-04-010-refine
capture-id: 2026-09-04-010-capture
learn-id: 2026-09-04-010
input-shape: candidate
interactionMode: autopilot
type: pattern
slug: sentinel-stub-red-gate-assertion-level
dup-status: none
resolution: none
lineage: null
status: complete
timestamp: 2026-09-04T20:20:00-04:00
---

# Refined Entry — sentinel-stub-red-gate-assertion-level

- **Type validation:** `pattern` is one of the four. Body carries the canonical
  `entry-schema.md` section template for `pattern` in order, with no extra top-level section that
  contradicts it (verified against the two pre-existing `pattern` files' headings).
- **Frontmatter validation:** `slug` kebab-case and unique; `domain` single and non-empty
  (`testing`) **and present in `tags`** per the coherence rule; `tags` within 2–6;
  `applicability` integers 0–10; `confidence` ∈ enum; `priority` ∈ enum;
  `source` = `{type: candidate, reference: 2026-09-04-003-review#2, extracted_at}`; `created_at` preserved from
  Capture; `updated_at` stamped now.
- **Coherence:** the body H1 kebab-cases to the `slug`; the index projection
  (filename/domain/tags/applicability/summary) is computable from the entry.
- **Duplicate + analog check (dedup-rules.md):**
- `test-config-isolation-behaviorally-not-regex` — `pattern`, domain `testing`, shared tags `testing` + `vitest` (>=2) -> **matched the analog pre-filter**, so the semantic comparison was performed per dedup-rules step 3. Problem/Solution differ in conclusion: that entry is about *how to prove two build/test projects are isolated* (run the tooling, assert outcomes, don't regex config text); this one is about *how to establish a Red that fails on AC assertions when the modules do not exist yet* (type-correct sentinel stubs). Different conclusions on the same domain -> **insignificant**, not an analog.
- `loose-casts-in-agent-tests-break-typecheck-wiring` — `gotcha` vs `pattern` -> insignificant (also recorded as a `related` link, since it explains why the stubs must compile).
- No exact slug match.
`dup-status: none` -> genuine new entry; **no user decision required**, so the autopilot run does not pause.
- Cross-linked both entries via `related` rather than merged; the conclusions stay separable.
- **Autopilot:** no exact/analog conflict and no validation failure, so nothing required a user
  decision — the run proceeds to Index without pausing.
